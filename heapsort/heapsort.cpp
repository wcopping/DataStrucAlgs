#include "max_heapify.h"
#include "build_heap.h"

// HEAPSORT(A)
//      BUILD-HEAP(A)
//      for i = length(A) to 2 do
//          exchange A[1] and A[i]
//          heap_size(A)--
//          HEAPIFY(A,1)
